import 'package:flutter/material.dart';

class PractitionerDietPage extends StatelessWidget {
  const PractitionerDietPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
        top: false,
        bottom: false,
        left: false,
        right: false,
        // child: PractitionerDietPageViewWidget(),
        child: Container(),
      );
  }
}
